version: "1"
scope: project

# Project-level MCP servers (added on top of global)
mcp: []
  # - name: project-db
  #   command: npx
  #   args: ["-y", "@modelcontextprotocol/server-postgres", "postgresql://localhost/mydb"]
  #   enabled: true

# Project-level skills (added on top of global)
skills: []
  # - name: deploy
  #   path: .coworker/skills/deploy
  #   enabled: true

# Project permissions (overrides global if set)
permissions:
  allow: []
  deny: []
