version: "1"
scope: global

# MCP Servers — shared across Claude Code, Gemini, OpenCode
mcp: []
  # Example:
  # - name: filesystem
  #   command: npx
  #   args: ["-y", "@modelcontextprotocol/server-filesystem", "/home/cicidi"]
  #   enabled: true
  #
  # - name: github
  #   command: npx
  #   args: ["-y", "@modelcontextprotocol/server-github"]
  #   env:
  #     GITHUB_PERSONAL_ACCESS_TOKEN: "your-token"
  #   enabled: true

# Skills
skills:
  - name: commit
    path: skills/commit
    description: Smart git commit with conventional commits format
    enabled: true

  - name: initiative-create
    path: skills/initiative-create/SKILL.md
    description: Create a new cross-project initiative with interview-based scaffolding
    enabled: true

  - name: initiative-edit
    path: skills/initiative-edit/SKILL.md
    description: Edit an existing initiative — add projects, decisions, links, docs
    enabled: true

# Permissions (Claude Code)
permissions:
  allow:
    - Bash(git *)
    - Bash(npm *)
    - Bash(python *)
    - Read(*)
    - Write(*)
  deny: []

# Claude Code specific
claude:
  effortLevel: medium
  skipDangerousModePermissionPrompt: false

# Gemini CLI specific
gemini:
  extra: {}

# OpenCode specific
opencode:
  extra: {}
