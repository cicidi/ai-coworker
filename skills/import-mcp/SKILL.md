---
name: import-mcp
description: Import MCP server config from .mcp.json into coworker.yaml
license: MIT
compatibility: claude-code,opencode,gemini
metadata:
  triggers:
    - import-mcp
  when_to_use: When user needs to import mcp server config from .mcp.json into coworker.yaml
  audience: ai-coworker
---

# import-mcp

Import MCP server config from .mcp.json into coworker.yaml.

## Usage

```bash
coworker import-mcp
```
