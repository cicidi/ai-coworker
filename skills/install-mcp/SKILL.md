---
name: install-mcp
description: Install an MCP server package via npm or pip
license: MIT
compatibility: claude-code,opencode,gemini
metadata:
  triggers:
    - install-mcp
  when_to_use: When user needs to install an mcp server package via npm or pip
  audience: ai-coworker
---

# install-mcp

Install an MCP server package via npm or pip.

## Usage

```bash
coworker install
```
