---
name: project-sync
description: Re-inject project catalog into IDE config files
license: MIT
compatibility: claude-code,opencode,gemini
metadata:
  triggers:
    - project-sync
  when_to_use: When user needs to re-inject project catalog into ide config files
  audience: ai-coworker
---

# project-sync

Re-inject project catalog into IDE config files.

## Usage

```bash
coworker project sync
```
